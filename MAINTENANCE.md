# 维护政策

本文档是 `knife4j-next` 面向使用者和贡献者的公开维护契约。具体版本已经交付的行为以对应源码、测试和 Release Notes 为准；如果某个版本需要偏离本政策，必须在发布前公开说明原因、影响和迁移方式。

## 项目定位

`knife4j-next` 是 `xiaoymin/knife4j` 的社区维护 fork，不是 upstream 官方仓库。它面向仍依赖 `doc.html`、既有 starter 和相关聚合能力的使用者，优先处理安全问题、回归、框架兼容与可重复发布，并以增量方式演进 OAS3 前端。

fork 是维持用户连续性的手段，不是必须永久分叉的目标。如果 upstream 恢复稳定维护和发布，本项目会根据实际兼容性、发布连续性和社区迁移成本重新评估边界；能够独立复用的修复会优先反馈 upstream。任何收窄、迁移或归档决定都应先公开说明，不会用突然删除仓库或已发布制品代替迁移方案。

## 当前支持范围

“最新稳定版”指已经完成本文[发布完成标准](#发布完成标准)的最新版本，而不只是已经创建的 tag。

| 范围 | 维护状态 | 承诺边界 |
| --- | --- | --- |
| Java / Maven 发布模块 | 主动维护 | 最新稳定版接受安全、回归与框架兼容修复；旧版本默认不回补，安全公告另有说明时除外 |
| OAS3 `front/core` + `front/ui-react` | 主线 | 承接规范消费、调试体验、UX 与向后兼容的新能力 |
| OAS2 `front/vue3` | 兼容维护 | 只接收安全补丁、回归和显示层缺陷，不扩张新产品能力 |
| Knife4x Go | 主动维护 | 维护最新稳定版宿主和其嵌入的 React UI；不单独复制一套前端产品线 |
| Knife4x Rust | 后置 | 尚未形成发布与支持承诺 |
| `legacy/**` | 冻结参考 | 不参与主线构建，不接收常规功能需求，也不视为受支持发布物 |
| 文档站 | 主动维护 | 描述当前稳定版和已合并能力；路线图不等同于交付承诺 |

没有列入上表的旧版本、实验代码和非发布模块，不因仍存在于 git 历史中而自动获得支持承诺。

## 兼容性契约

从 upstream 迁移到本项目时，Maven `groupId` 改为 `com.baizhukui` 是明确的交付身份变化。除这项已公布的变化外，维护时优先保留以下外部契约：

- 既有 Maven `artifactId` 与 starter 职责；
- Java 包名 `com.github.xiaoymin.knife4j.*`；
- `knife4j.*` 配置前缀和已经文档化的配置语义；
- `/doc.html`、对应 starter 的 API docs 入口与常用聚合行为；
- 默认情况下可继续工作的应用接入方式，以及前端已文档化的用户数据和设置行为。

兼容不等于接受所有历史输入。违反其声明 OpenAPI / Swagger 规范、无法安全解释或存在安全风险的文档，可以被明确拒绝；项目不会为了把无效输入伪装成合法契约而增加猜测式 alias 或静默转换。

以下情况可能需要改变既有行为：修复安全漏洞、纠正明确违反规范的行为、适配已经进入支持范围的框架变化，或实施经公开讨论确认的产品调整。若变化会影响正常使用，必须同时提供：

1. 可检索的 issue 或设计背景；
2. 影响范围与受影响版本；
3. Release Notes 与迁移方法；
4. 可行时的回滚或临时规避方式；
5. 与影响相称的版本号和验证证据。

## 变更如何进入主线

- 安全问题优先于一般功能，按 [SECURITY.md](./SECURITY.md) 的私密流程处理。
- Bug / 回归在修复前先建立可重复的复现证据，修复后保留针对同一场景的回归验证。
- 新产品能力只进入 OAS3 主线，优先保持可选、可回滚，不借机重写无关模块。
- Java、框架和依赖兼容变更需要覆盖受影响的真实版本组合，不能只用单一编译结果代替运行时证据。
- 每个 PR 应围绕一件可独立验证的事，并在合入前经过 CI 和实现上下文之外的审查。

## 发布完成标准

tag、源码 CI、公开制品和在线 Demo 是不同层次的事实。一个 Java 稳定版本只有同时满足以下条件，才视为发布完成：

1. tag 指向经过验证的准确源码；
2. 正式 Release workflow 在该 tag 上成功；
3. Maven Central 上的 POM、JAR、源码包、Javadoc 和签名可按准确坐标获取并通过核验；
4. GitHub Release 已发布，正文与仓库内 Release Notes 一致；
5. 在线 Demo 使用同一正式版本，并且其部署发生在公开制品核验之后；
6. 隔离环境能只从 Maven Central 消费该版本完成最小启动验证。

单独出现 tag、某个 job 变绿或仓库内版本号变化，都不能独立证明发布完成。Knife4x 等非 Maven 发布物沿用同一原则：源码、制品、校验信息和可运行结果必须能对应到同一个不可变版本。

## 节奏与响应预期

项目当前由有限的社区维护能力支撑，不承诺固定发布日期、首次响应时间或修复 SLA。一般修复会按风险和相互依赖关系批量交付；高影响安全问题会尽力优先处理。公开 issue 的 label、里程碑、PR 和 Release 才是当前状态依据，路线图中的候选项不代表已经排期。

## 维护连续性

如果现有维护能力不足以继续满足上述边界，维护者应通过公开 issue 或公告说明：受影响范围、最后受支持版本、已知风险、可迁移去向和仓库后续状态。项目不会用长期沉默来暗示仍在正常维护，也不在没有实际多人治理能力时宣称基金会或组织化治理。
